window.CLOUD_CONFIG = {
  enabled: true,

  // Supabase project URL — Settings → API → Project URL
  supabaseUrl: 'PASTE_SUPABASE_URL',

  // Supabase anon public key — Settings → API → anon public
  supabaseKey: 'PASTE_SUPABASE_ANON_KEY',

  autoSync: true,
  syncDebounceMs: 700
};
